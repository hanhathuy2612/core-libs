subprojects {
    apply plugin: 'java'
    apply plugin: 'java-library'
    apply plugin: 'maven-publish'
    apply plugin: 'idea'

    sourceCompatibility = '17'

    repositories {
        mavenCentral()
    }
}